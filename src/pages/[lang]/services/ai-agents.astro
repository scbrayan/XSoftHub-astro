---
import ServicePage from "../../../components/ServicePage.astro";
import { getStaticPaths, useTranslations } from "../../../i18n/utils";
import type { SupportedLanguage } from "../../../types";
import { ASSETS_IMG_SERVICES } from "../../../utils";

export { getStaticPaths };

const { lang } = Astro.params;
const currentLang = lang as SupportedLanguage;
const t = useTranslations(currentLang);
---

<ServicePage
  lang={currentLang}
  title={t("services.ai_agents.title")}
  subtitle={t("services.ai_agents.subtitle")}
  description={t("services.ai_agents.description")}
  features={t("services.ai_agents.features") as string[]}
  image={`${ASSETS_IMG_SERVICES}${t("services.ai_agents.image")}`}
  imageAlt="AI Agents Service"
/>
